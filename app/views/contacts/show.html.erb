<p id="notice"><%= notice %></p>
<div class="container">
  <div class="row">
    <div class="col-md-12">
<p>
  <strong><%= (t 'contacts.name') %>:</strong>
  <%= @contact.email %>
</p>

<p>
  <strong><%= (t 'contacts.email') %>:</strong>
  <%= @contact.name %>
</p>

<p>
  <strong><%= (t 'contacts.phone') %>:</strong>
  <%= @contact.phone %>
</p>

<p>
  <strong><%= (t 'contacts.content') %>:</strong>
  <%= @contact.content %>
</p>

<%= link_to (t 'button.edit'), edit_contact_path(@contact), class: "btn btn-sm btn-warning" %> |
<%= link_to (t 'button.back'), root_path, class: "btn btn-sm btn-primary" %>
    </div>
  </div>
</div>
